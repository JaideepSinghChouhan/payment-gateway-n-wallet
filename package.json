{
  "name": "payment-gateway-n-wallet",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace=payment-gateway-backend",
    "dev:frontend": "npm run dev --workspace=frontend"
  }
}
